import { Routes } from '@angular/router';
import { PublicPageComponent } from './public-page.component';

export const publicPageRoutes: Routes = [
    { path: '', component: PublicPageComponent },
];